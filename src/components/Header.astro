---
/**
 * Header — Fixed nav with logo, desktop links, CTA, and mobile hamburger.
 * Transitions from transparent to blur+dark after 60px scroll.
 *
 * The logo uses the actual azedin-logo.jpg (small, 2.9KB) — displays it
 * at a tasteful size in the header. We treat it as a raster logo since
 * vectorizing in Astro build is complex.
 */
import logoSrc from '../assets/img/logo-azedin-barber-transparente.png';
import CTA from './CTA.astro';

const navLinks = [
  { href: '#lookbook',  label: 'Lookbook'  },
  { href: '#servicios', label: 'Servicios' },
  { href: '#reseñas',   label: 'Reseñas'   },
  { href: '#ubicacion', label: 'Ubicación' },
];
---

<header
  id="site-header"
  class="fixed top-0 left-0 right-0 z-[100] transition-[background-color,backdrop-filter] duration-300"
  style="transition-timing-function: var(--ease-out);"
>
  <div class="flex items-center justify-between px-6 md:px-10 h-[68px]">

    <!-- Logo -->
    <a href="#hero" class="flex items-center group" aria-label="Azedin Barber — Inicio">
      <img
        src={logoSrc.src}
        alt="Azedin Barber"
        class="nav-logo-img"
        loading="eager"
        width="500"
        height="333"
      />
    </a>

    <!-- Desktop navigation -->
    <nav class="hidden md:flex items-center gap-8" aria-label="Navegación principal">
      {navLinks.map(link => (
        <a
          href={link.href}
          class="text-[11px] uppercase tracking-[0.16em] font-semibold text-ink-dim hover:text-cream transition-colors duration-180"
          style="transition-timing-function: var(--ease-out);"
        >
          {link.label}
        </a>
      ))}
    </nav>

    <!-- CTA + hamburger -->
    <div class="flex items-center gap-4">
<CTA
          label="Reservar cita"
          extraClass="hidden md:inline-flex text-[10px] px-5 py-2.5"
        />

      <!-- Mobile hamburger -->
      <button
        data-nav-trigger
        class="md:hidden flex flex-col items-center justify-center gap-[4px] w-[44px] h-[44px] bg-transparent border-none cursor-pointer group"
        aria-label="Abrir menú"
        aria-expanded="false"
        aria-controls="mobile-nav"
      >
        <span class="block w-5 h-[2px] bg-cream rounded-full transition-all duration-200 group-hover:bg-oak"></span>
        <span class="block w-5 h-[2px] bg-cream rounded-full transition-all duration-200 group-hover:bg-oak"></span>
        <span class="block w-5 h-[2px] bg-cream rounded-full transition-all duration-200 group-hover:bg-oak"></span>
      </button>
    </div>

  </div>

  <!-- Bottom separator line -->
  <div class="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-border to-transparent opacity-0 transition-opacity duration-300" id="header-line"></div>
</header>



<script>
  document.addEventListener('DOMContentLoaded', () => {
    const trigger = document.querySelector('[data-nav-trigger]');
    if (!trigger) return;
    window.addEventListener('nav:open', () => {
      trigger.setAttribute('aria-expanded', 'true');
    });
    window.addEventListener('nav:close', () => {
      trigger.setAttribute('aria-expanded', 'false');
    });
  });
</script>

<style>
  .nav-logo-img {
    width: 110px;
    height: 44px;
    object-fit: contain;
    
    filter: invert(1);
    opacity: 0.94;
    transition: opacity 200ms var(--ease-out), transform 200ms var(--ease-out);
  }
  
  .nav-logo-img:hover {
    opacity: 1;
    transform: scale(1.03);
  }
</style>
